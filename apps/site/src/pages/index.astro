---
import games from '../../../../games.json';
import Catalogue from '../layouts/Catalogue.astro';
---

<Catalogue title="Games — LVTD Games">
  <h1>Games</h1>
  <ul class="games" aria-label="Game collection">
    {games.map((game) => (
      <li>
        <a class="game" data-game={game.slug} href={`/${game.slug}/`}>
          <img class="logo" src={game.logo} alt="" width="104" height="104" />
          <div class="details">
            <h2 lang={game.language}>{game.title}</h2>
            <p>{game.description}</p>
          </div>
          <span class="play">
            Play <span aria-hidden="true">→</span>
          </span>
        </a>
      </li>
    ))}
  </ul>
</Catalogue>

<style>
  .games {
    display: grid;
    gap: 20px;
    margin: 0;
    padding: 0;
    list-style: none;
  }
  .game {
    display: flex;
    align-items: center;
    gap: 28px;
    padding: 28px;
    border: 1px solid var(--border);
    border-radius: 20px;
    background: var(--surface);
    transition:
      background-color 160ms,
      border-color 160ms;
  }
  .game:hover {
    background: var(--surface-hover);
    border-color: var(--purple);
  }
  .logo {
    flex: none;
    border-radius: 22px;
  }
  h2 {
    margin: 0;
    font-size: clamp(24px, 3vw, 32px);
    font-weight: 700;
    letter-spacing: -0.8px;
  }
  .details {
    min-width: 0;
  }
  p {
    margin: 10px 0 0;
    color: var(--lavender);
    font-size: 17px;
    line-height: 1.5;
  }
  .play {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 24px;
    margin-left: auto;
    padding: 16px 24px;
    border-radius: 12px;
    background: var(--purple);
    font-size: 17px;
    font-weight: 700;
  }
  .play span {
    font-size: 23px;
    line-height: 1;
  }
  @media (max-width: 600px) {
    .game {
      display: grid;
      grid-template-columns: 72px minmax(0, 1fr);
      gap: 20px;
      padding: 20px;
    }
    .logo {
      width: 72px;
      height: 72px;
      border-radius: 16px;
    }
    h2 {
      overflow-wrap: anywhere;
    }
    .details {
      display: contents;
    }
    p {
      grid-column: 1 / -1;
      margin: 0;
    }
    .play {
      grid-column: 1 / -1;
      margin: 0;
      justify-content: space-between;
      padding: 14px 18px;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .game {
      transition: none;
    }
  }
</style>

<script>
  import { track } from '../lib/analytics.js';
  document
    .querySelectorAll<HTMLAnchorElement>('[data-game]')
    .forEach((link) => {
      const capture = () =>
        track('game_selected', { target_game: link.dataset.game });
      link.addEventListener('click', capture);
      link.addEventListener('auxclick', (event) => {
        if (event.button === 1) capture();
      });
    });
</script>
